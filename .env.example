# ABL Vertex Solutions
#
# The database/backend binding is provided automatically by the Lovable Cloud
# project — you do NOT need to configure any database URL or API keys manually.
#
# The only project-managed secret is the admin session signing key, which is
# already stored securely in the Cloud project:
#   ADMIN_SESSION_SECRET
